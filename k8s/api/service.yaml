apiVersion: v1
kind: Service
metadata:
  name: orbitx-api-service
  namespace: orbitx
  labels:
    app: orbitx-api
spec:
  type: ClusterIP
  selector:
    app: orbitx-api
  ports:
    - port: 8000
      targetPort: 8000
      name: http
